# Artisan — Living Context

Purpose: a single, always-current snapshot of what actually exists in this codebase right now. Update this file after every major milestone — not chronologically-append-only, but rewritten so it always reflects current state. Git history is the record of *how* we got here; this file is the record of *where we are*.

## Current Status (as of 2026-08-31)

**Stage: Sprints 1–7 are all closed, merged, and codified.** Sprints 1–5 (foundations, Gate 1 intake, Gate 2 plan→execute→verify→PR, Gate 3 merge-conflict triage, monitoring dashboard) and Sprint 6 (observability, IAM tightening, cap/idempotency audit, live happy path) are confirmed live. Sprint 7 is fully closed: multi-stage Dockerfiles for all three services (`orchestrator`, `execution-sandbox`, `dashboard`), complete declarative Terraform infrastructure in `infra/terraform/`, one-command idempotent bootstrap in `infra/scripts/setup-gcp-infra.sh`, Secret Manager integration for dashboard OAuth credentials (`infra/scripts/create-dashboard-secrets.sh`), CI/CD automation (`.github/workflows/ci.yml` and `.github/workflows/deploy.yml`) covering all three components, and deployment documentation. Next: Sprint 8 (Post-Sprint Activity) — Demo, Docs & Submission.

Sprint 5 built `dashboard/` from its Sprint-1 scaffold into a working Next.js 15 + Auth.js v5 app: card-grid ticket overview with status-bucket filters (covering the "awaiting human" statuses), full gate-by-gate drill-in, and SSE-based live updates — see Milestone 8 for the full account, including two backend fixes folded into this sprint (`trace_ids` was previously dead code; a new `current_step` field gives the dashboard a live progress signal) and two real bugs found during Playwright verification against the actual production build (a Turbopack/Auth.js middleware interaction; `trustHost` needed for any non-default host). 147 tests passing across `agents/` (79), `packages/artisan_shared/` (13), `execution-sandbox/` (29), and `dashboard/` (20 Vitest + 6 Playwright).

Sprint 2/3 history:

**Proven live, both Gate 1 branches (Sprint 2):**
- **Sufficient path:** issue #2 on `403errors/artisan-demo` ("Password reset..."-style, well-specified) → webhook → Pub/Sub → ticket bootstrap → real Gemini call (Vertex AI) → `sufficient: true` → Jira `ART-8` → *In Progress*, Firestore `status: in_progress`.
- **Insufficient path:** issue #3 ("Login is broken", deliberately vague) → real Gemini call → specific clarifying question posted as a GitHub comment → `clarification_rounds` incremented to 1 in Firestore → Jira `ART-9` correctly left untouched (`Backlog`).

Getting the sufficient path live for the first time required two more real bugs to surface and get fixed (see "Known follow-up" below): a missing Vertex AI wiring (Gemini access was never actually configured — no API enabled, no IAM, no env vars) and a Pub/Sub infinite-retry incident (no dead-letter policy) that had been silently running for ~95 minutes before being caught.

- [PRD.md](./PRD.md), [SYSTEM_DESIGN.md](./SYSTEM_DESIGN.md), [TECH_STACK.md](./TECH_STACK.md), and [SPRINT.md](./SPRINT.md) are finalized and reflect the full v1 vision (all 3 gates, full dashboard), phased into 8 sprints. [MILESTONE.md](./MILESTONE.md) holds the closed-sprint (1–5) phase-by-phase DoD detail that used to live in `SPRINT.md`.
- Repo layout: single monorepo (`agents/`, `execution-sandbox/`, `packages/artisan_shared/`, `dashboard/`, `infra/`, `docs/`) — `agents/`, `execution-sandbox/`, and `packages/artisan_shared/` are three members of one `uv` workspace as of Sprint 3 (see Milestone 4).
- Target submission: All Things Agentic Hackathon 2026, deadline August 31, 2026.

## What Exists Right Now

- `agents/` (uv, Python 3.13): Sprint 1's scaffold (`config.py`, `models.py` — now 7 typed contracts including `GitHubWebhookEnvelope`, `firestore_schema.py`) plus Sprint 2's orchestrator code:
  - `app.py` — FastAPI service, two routes: `POST /webhooks/github` (HMAC signature verification, publishes to Pub/Sub) and `POST /pubsub/push` (OIDC push-token verification, idempotency check, dispatch).
  - `dispatch.py` — Gate 1 control flow: ticket bootstrap, Intake Agent invocation, clarification loop with the 3-round cap, gate-decision tracing.
  - `gcp/` — `secrets.py` (Secret Manager), `pubsub.py` (publish + push-token verification), `firestore_client.py` (ticket CRUD + the transactional `clarification_rounds` cap + the `processed_deliveries` idempotency guard — reworked in Sprint 3's close-out into `claim_delivery`/`mark_delivery_completed`/`mark_delivery_failed`, a claim made *before* processing rather than a flag set after; see "Known follow-up" below).
  - `github/` — `webhook.py` (signature verify + envelope parsing), `auth.py` (App JWT → installation token via `githubkit`'s `AppAuthStrategy`), `client.py` (post comment, read issue thread).
  - `jira/client.py` — direct Jira Cloud REST API v2 calls (`httpx`, Basic Auth with `ARTISAN_JIRA_USERNAME` + the `jira-api-token` secret). Originally implemented against `mcp-atlassian` over MCP; switched after a live field-test failure (see "Known follow-up" below).
  - `agents/intake_agent.py` — real ADK `Agent` (`output_schema=IntakeVerdict`), run per-invocation via a fresh `Runner`/`InMemorySessionService` session (stateless between calls, per SYSTEM_DESIGN.md §7).
  - `tracing.py` — OTel `TracerProvider` + `CloudTraceSpanExporter`, one span per gate decision.
  - `Dockerfile` + `.dockerignore` — multi-stage `uv` build, deployed to Cloud Run as the `orchestrator` service.
  - Plus Sprint 3's Gate 2 additions: `gate2.py` (routing → domain-expert dispatch → plan/execute/verify retry loop → PR+Jira sync, mirroring `dispatch.py`'s style); `agents/` (agent package) now also has `routing_agent.py`, `domain_expert_agent.py`, `planning_agent.py`, `verification_agent.py`, and a shared `_run_agent.py` helper factoring out the fresh-session-per-call boilerplate all five reasoning agents share; `gcp/cloud_run_jobs.py` (triggers + awaits the `execution-sandbox` Cloud Run Job synchronously via `run_job(...).result()`); `gcp/firestore_client.py` gained `increment_retry_round`/`RetryCapExceeded` (mirrors the clarification cap exactly) and `append_escalation` (atomic `firestore.ArrayUnion`, not read-modify-write); `github/client.py` gained `open_pull_request`.
  - Plus Sprint 4's Gate 3 additions: `gate3.py` (detection → Conflict Agent classification → trivial-resolution-or-semantic-escalation, mirroring `gate2.py`'s style — but no retry loop, since Phase 4.3's cap is exactly 1 attempt); `agents/agents/conflict_agent.py` (mirrors `routing_agent.py`'s shape, `output_schema=ConflictVerdict`); `dispatch.py` gained a `pull_request`/`opened`+`synchronize` branch handing off to `gate3.handle_pull_request_event`; `gate2.py`'s `_open_pr_and_sync` now captures the PR number it previously discarded and writes it to `firestore_client.write_pr_pointer`; `gcp/firestore_client.py` gained `increment_trivial_conflict_attempt`/`TrivialConflictCapExceeded` (claims the attempt *before* it runs — `>` not `>=`, see Current Status above), `write_pr_pointer`/`get_ticket_by_pr` (the `pr_index` pointer-doc lookup); `gcp/cloud_run_jobs.py` gained `trigger_conflict_detection`/`trigger_conflict_resolution` + `ConflictDetectionCrashed` (a crashed detection has no honest default to synthesize, unlike a crashed execution — it's raised, not silently faked).
  - Pinned deps unchanged from Sprint 2 plus workspace-local `artisan-shared` (see below); `google-cloud-run==0.16.1` (pinned since Sprint 1, unused until now) is what `cloud_run_jobs.py` uses.
  - 75 passing pytest tests as of Sprint 4's code-complete point (`uv run --package artisan-agents pytest agents/tests`; 54 at Sprint 3's close-out, +21 net for Gate 3: the Conflict Agent, `gate3.py`'s control-flow suite covering untracked-PR-no-op / no-conflict / semantic-escalation / trivial-success / trivial-forced-failure-with-exactly-one-attempt / detection-crash, the new cap/PR-index Firestore integration tests, the new `cloud_run_jobs` trigger tests, and the `dispatch.py`/`gate2.py` wiring tests).
- `packages/artisan_shared/` (new in Sprint 3, uv, Python 3.13): `models.py` (all typed inter-agent I/O, including `RoutingDecision` and, as of Sprint 4, `ConflictDetectionResult`), `firestore_schema.py` (`TicketDoc`, now with `last_execution_result`, `pr_number`, `trivial_conflict_attempts`, `last_conflict_detection`, `last_conflict_resolution` fields), `ticket_ids.py` (`ticket_doc_id` plus, as of Sprint 4, `pr_pointer_doc_id`), `secrets.py` (`fetch_secret`, parameterized on project id), `github_auth.py` (`build_installation_client` + `mint_installation_token`) — extracted from `agents/` so `execution-sandbox` doesn't duplicate this logic; both `agents/` and `execution-sandbox/` depend on it via a `uv` workspace (root `pyproject.toml`). 13 passing tests.
- `execution-sandbox/` (built from scratch in Sprint 3 — previously a bare stub with zero logic): `main.py` (Cloud Run Jobs entrypoint, `JOB_MODE`-branched as of Sprint 4 — `execute` (Gate 2, default), `detect_conflict`/`resolve_conflict` (Gate 3) — always exits 0 since a failed attempt is data not a crash), `git_ops.py` (subprocess `git` CLI wrappers — clone/branch/stage+diff/commit/push, with token redaction on the push path; Sprint 4 added `checkout`/`fetch`/`merge`/`list_conflicted_files`/`read_conflict_markers`/`log_for_paths` for the conflict-detection/resolution jobs), `coding_agent.py` (the bounded ADK coding agent — see "Known follow-up" below for why it doesn't use ADK's built-in `bash_tool`; Sprint 4 extracted the ADK Runner/session boilerplate into a shared `_run_bounded_agent` helper and added `run_conflict_resolution_agent`, a sibling to `run_coding_agent` with a conflict-marker-shaped prompt instead of a `Plan`'s), `test_runner.py` (single configured test command), `github_auth.py`/`secrets.py` (thin bindings onto `artisan_shared`), `firestore_write.py` (Sprint 4 added `write_conflict_detection_result`/`write_conflict_resolution_result` alongside the existing `write_execution_result`, through a shared `_update` helper). `Dockerfile` added (mirrors `agents/Dockerfile`'s shape, plus installs `git` in the final image). 28 passing tests, including real-local-git-repo tests for `git_ops.py` (no network/GCP dependency, including two real diverging-branch fixtures — one non-overlapping/clean-merge, one same-line-conflicting — that double as Sprint 8 demo rehearsal material per SPRINT.md's instruction) and a test proving the coding agent's tool-call ceiling terminates a stuck model instead of hanging.
- `dashboard/`: unchanged since Sprint 1.
- Firestore native-mode database in `artisan-multiagent-ai` (region `us-central1`) — now also holds a top-level `processed_deliveries` collection (Phase 2.1's pre-ticket idempotency guard, reworked in Sprint 3's close-out into a claim/`in_progress`/`completed`/`failed` lifecycle) and, as of Sprint 4, a top-level `pr_index` collection (`{repo}__{prNumber} -> {ticket_doc_id}`, the same deterministic-id-not-a-query philosophy) alongside `tickets/`.
- **Deployed to GCP:** Pub/Sub topic `artisan-github-events` + push subscription `artisan-github-events-push` (ack deadline raised to 600s, its ceiling, during Sprint 3's close-out — extra headroom on top of the claim-based idempotency fix, since Gate 2 can run for minutes); orchestrator Cloud Run service (public ingress for `/webhooks/github`, in-app OIDC check protects `/pubsub/push`; request timeout raised to 3600s in Sprint 3's close-out so a real Gate 2 run is never cut off mid-pipeline); GitHub App webhook URL pointed at the deployed orchestrator; `execution-sandbox` registered as a Cloud Run Job for the first time in Sprint 3's close-out (`--task-timeout=1800`, own service account). `mcp-atlassian` was deployed in Sprint 1 and **deleted** in Sprint 2 once superseded (see below) — it pulled a public image, so nothing custom was lost. Both `orchestrator` and `execution-sandbox` images were built via Cloud Build (no local Docker daemon in this environment) in the `cloud-run-source-deploy` Artifact Registry repo, tagged from the working-tree state at Sprint 3's close-out (ahead of the last commit): `execution-sandbox:a89aa31-dirty`; `orchestrator:a89aa31-dirty2` (a second build, after catching and fixing the `DELIVERY_CLAIM_STALE_AFTER_SECONDS` value below — the first `orchestrator:a89aa31-dirty` build is stale, superseded, not the deployed image).

## Known follow-up: mcp-atlassian dropped after a live field-test failure

Sprint 1 deferred live MCP-protocol verification of `mcp-atlassian` to "once the orchestrator exists and calls it Cloud-Run-to-Cloud-Run." That happened in Sprint 2 — and surfaced a real, unresolved bug in the pinned `sooperset/mcp-atlassian:0.23.1` image itself, not a config or credentials problem on our side:

- Networking was fine once `mcp-atlassian`'s ingress was opened from `internal` to `all` (Cloud Run's `ingress: internal` only accepts traffic that routes through Google's internal VPC network, and by default a Cloud Run service's outbound calls to *another* Cloud Run service's public URL do **not** count as internal, even same-project — this needs a Serverless VPC Access connector to actually work as "internal," which wasn't provisioned. Since IAM auth — `roles/run.invoker` scoped to `orchestrator@` only — still fully gated access, opening ingress didn't weaken the actual security property).
- Once reachable, the real MCP session (initialize → tools/call) worked correctly, and `mcp-atlassian` correctly routed the `jira_create_issue` tool call.
- But `mcp-atlassian` itself then failed with `401 Unauthorized` calling Atlassian's own `/rest/api/2/myself` — **with two different, independently-verified-valid API tokens** (each confirmed working via a direct `curl -u email:token` REST call, both immediately after rotating them in Secret Manager and after forcing a fresh Cloud Run revision to pick up each new secret version). This rules out a bad/expired token; the failure is inside `mcp-atlassian`'s own Jira-auth code path (its traceback shows it runs Jira calls through an SSRF-protection hook, `attach_ssrf_hook=True`, which appears to interfere with outbound Basic Auth in this environment) — a third-party image bug, not something fixable from our side without much deeper investigation.

**Resolution:** `jira/client.py` now calls the Jira Cloud REST API directly (same credentials, same Secret Manager token, Basic Auth) — proven reliable throughout this diagnosis. `mcp-atlassian` was deleted. If MCP-based tool access is ever wanted again (e.g. for a future domain-expert agent), it would need either a from-scratch re-implementation or a different pinned image version, and should be smoke-tested against real Jira auth *before* being wired into the orchestrator's critical path again.

## Known follow-up: Sprint 2 infra/deployment — done; Gate 1 verified live end-to-end

Deployed: Pub/Sub topic + subscription (+ dead-letter topic, see below), orchestrator Cloud Run service, IAM grants (`orchestrator@` → `iam.serviceAccountTokenCreator` for Pub/Sub's push-service-agent, needed for push-auth token minting — this was *not* auto-granted by `--push-auth-service-account` and had to be added explicitly; `roles/aiplatform.user` for Vertex AI, see below), GitHub App webhook URL.

**Verified live:** webhook signature verification (401 on forged signature); Pub/Sub publish → push delivery → OIDC verification → dispatch; ticket bootstrap end-to-end — `issues opened` → Firestore ticket doc created → real Jira ticket created via direct REST; the Intake Agent's real Gemini call (both `sufficient` and `insufficient` verdicts); `github_client.get_issue_thread`/`post_issue_comment` against real issues (`#2`, `#3` on `403errors/artisan-demo`); `jira_client.transition_ticket`/`add_comment` against real tickets (`ART-8` → In Progress, `ART-9` left untouched). Gate 1 is now fully closed — no remaining "unit-tested only" gap.

## Known follow-up: Gemini access was never actually wired up (found + fixed Sprint 2)

The very first live test of the Intake Agent's real Gemini call failed with `ValueError: No API key was provided.` Investigation found neither `aiplatform.googleapis.com` (Vertex AI) nor `generativelanguage.googleapis.com` (Gemini Developer API) was enabled on the project at all — this had been silently assumed done since Sprint 1, but was never actually a completed prerequisite, and no unit test could have caught it since unit tests mock the model.

**Resolution (Vertex AI, no new secret needed — consistent with the rest of the architecture's "orchestrator's own service account" pattern):**
- Enabled `aiplatform.googleapis.com`.
- Granted `orchestrator@` → `roles/aiplatform.user`.
- Added Cloud Run env vars: `GOOGLE_GENAI_USE_VERTEXAI=TRUE`, `GOOGLE_CLOUD_PROJECT=artisan-multiagent-ai`, `GOOGLE_CLOUD_LOCATION=global`.

That last one caught a second, non-obvious bug: with `GOOGLE_CLOUD_LOCATION=us-central1` (matching every other resource in this project), Gemini calls 404'd with "Publisher model ... was not found or your project does not have access to it" — even though the model *was* visible in `client.models.list()` and the Cloud Console Model Garden page for it showed no access/enablement gate at all. Confirmed via a standalone script that `gemini-2.5-flash` worked fine on `us-central1` while `gemini-3.7-flash` only responds on Vertex AI's **`global`** location. `TECH_STACK.md`/`SYSTEM_DESIGN.md` now call this out explicitly since it's not discoverable from the Console UI.

## Known follow-up: Pub/Sub infinite-retry incident (found + fixed Sprint 2)

While field-testing the above, discovered the orchestrator had been in a continuous Pub/Sub redelivery storm since 01:47 UTC — **~95 minutes and 4,400+ failed requests** by the time it was caught — because the push subscription had no dead-letter policy and no retry-policy backoff cap configured. Two Firestore tickets were stuck permanently retrying: `ART-7` (from synthetic test issue `990006`, which never existed on GitHub — a 404 that could never resolve, retried forever) and transiently `ART-8` (issue #2 itself, before the Vertex AI fix).

**Resolution:** created dead-letter topic `artisan-github-events-dlq`, wired the required IAM (Pub/Sub service agent → `roles/pubsub.publisher` on the DLQ topic, `roles/pubsub.subscriber` on the source subscription), and set `max-delivery-attempts=5` on `artisan-github-events-push`. `ART-7`/the `990006` Firestore ticket was manually marked `manual_pickup` and the Jira ticket commented + transitioned to Done, same as the `ART-1`–`ART-6` cleanup. **This is a real gap in Phase 2.1's failure-handling story** (a non-retriable failure — like a GitHub 404 that will never resolve — must not be allowed to retry forever just because the handler raises rather than terminating gracefully); worth a proper code-level fix in Sprint 6's hardening pass, not just the infra-level mitigation done here.

## Known follow-up: Jira's real workflow has no "PR Open — Awaiting Review" status (found Sprint 3)

Before implementing Phase 3.6, queried the real transitions available on `ART-8`/`ART-9` (both `In Progress`) via a live `GET /rest/api/2/issue/{key}/transitions` call, the same way Sprint 2 discovered "In Progress" was the real status name for Gate 1. Result: this Jira site's team-managed Kanban workflow only exposes **four** statuses — `Backlog`, `Selected for Development`, `In Progress`, `Done` — with no PR-review-shaped status in between, despite that being the PRD's/`SYSTEM_DESIGN.md`'s phrasing for the Gate 2 PR-open state.

**Resolution:** Gate 2 (`agents/src/artisan_agents/gate2.py`) never attempts a Jira status transition on the PR-open path. The ticket stays `In Progress` in Jira; the PR link and diff summary are communicated via a Jira comment instead (`jira_client.add_comment`, already existing and generic). Firestore's own `TicketDoc.status` field still tracks `"pr_open"` exactly as `SYSTEM_DESIGN.md` §6.4 specifies — it, not Jira's coarser 4-status workflow, is the real source of truth for this state. `docs/SYSTEM_DESIGN.md` §4 and `docs/MILESTONE.md` Phase 3.6 updated to reflect this. If a real "PR Open"-shaped Jira status is wanted later, it would require a Jira admin reconfiguring this project's workflow scheme (outside this repo's control, and not attempted here without being asked).

## Known follow-up: ADK's built-in `bash_tool` can't run unattended (found Sprint 3)

While designing `execution-sandbox`'s coding step, initially planned to reuse ADK's built-in `google.adk.tools.bash_tool.ExecuteBashTool` (it ships with a `BashToolPolicy` for command allowlisting/resource limits, which looked like free sandboxing). Reading its `run_async` implementation before committing to it surfaced a blocker: it unconditionally calls `tool_context.request_confirmation(...)` before every single command, with no constructor flag or policy setting to disable this — it's built for an interactive human-in-the-loop flow. In an unattended Cloud Run Job with no human present to approve, every tool call would return "this tool call requires confirmation" forever, since nothing ever confirms it.

**Resolution:** `execution-sandbox/src/artisan_execution_sandbox/coding_agent.py` uses plain Python functions (`read_file`/`write_file`/`list_directory`/`run_shell_command`/`finish`) as tools instead. ADK wraps plain callables as `FunctionTool`, which defaults `require_confirmation=False` — confirmed by reading `function_tool.py` and by a local spike script exercising the full multi-round function-calling loop (ADK's `base_llm_flow.run_async` loops `_run_one_step_async`, one LLM call per round, until the model's response contains no more function calls — confirmed this happens automatically within a single `runner.run_async()` invocation, which `intake_agent.py`'s existing usage never actually exercised since it has no tools at all). A `MAX_CODING_AGENT_TOOL_CALLS` cap (40) stops a stuck model from looping past the job's own execution timeout — tested with a fake model that always requests another tool call.

## Known follow-up: a duplicate-delivery race in the idempotency guard (found + fixed Sprint 3 close-out)

While planning Sprint 3's live end-to-end test, re-read `app.py`'s `/pubsub/push` handler and found a real bug that had been live since Sprint 2, just never triggered: the push subscription's `ackDeadlineSeconds` was `10`, but the delivery was only marked "seen" (`mark_delivery_processed`) *after* `handle_event` fully returned — for Gate 1 that's fast enough to rarely matter, but Gate 2 can run for minutes (several LLM calls plus a full Cloud Run Job execution). Pub/Sub redelivering the same message while the first attempt is still mid-flight would have hit `is_duplicate_delivery` before it was ever marked, and re-run the entire Gate 2 pipeline concurrently — a second PR, a second Jira comment, etc.

**Resolution:** idempotency is now a claim, not a flag. `gcp/firestore_client.py`'s `is_duplicate_delivery`/`mark_delivery_processed` were replaced with `claim_delivery`/`mark_delivery_completed`/`mark_delivery_failed`, following the exact transactional commit-then-decide shape already used for the `clarification_rounds`/`retry_count` caps. A `processed_deliveries/{delivery_id}` doc's `status` moves `in_progress` → `completed` (permanent dedupe) or `in_progress` → `failed` (immediately reclaimable, so Pub/Sub's own retry-on-genuine-failure still works); a stale `in_progress` claim older than `DELIVERY_CLAIM_STALE_AFTER_SECONDS` (70 min — deliberately longer than the orchestrator's 3600s/60min max request timeout, so a claim never goes stale while the underlying request could still legitimately be running) is also reclaimable, so a Cloud Run instance dying mid-request can't block that delivery forever. `app.py`'s handler now claims before calling `handle_event`, and marks failed/completed in a `try`/`except` around it, re-raising so Pub/Sub's dead-letter policy still sees genuine failures. As additional (secondary) headroom, the push subscription's `ackDeadlineSeconds` was also raised to 600 (Pub/Sub's ceiling) and the orchestrator's own Cloud Run request timeout to 3600 — neither is what actually fixes the race, but both reduce redundant redelivery traffic while Gate 2 is genuinely still working. See `docs/SYSTEM_DESIGN.md` §7/§6.4 for the updated schema description.

## Known follow-up: `orchestrator@` was missing the IAM role for Cloud Trace (found + fixed Sprint 3 close-out; independently re-checked Sprint 4 close-out — IAM was necessary but not sufficient)

During the Sprint 3 close-out's live end-to-end test, checked the Cloud Trace API directly for the `gate: "1"`/`gate: "2"` spans Phase 2.5/3.7 claim as done — found zero traces, and orchestrator logs showing `PermissionDenied: 403 Permission 'cloudtrace.traces.patch' denied` on every span export attempt, going back at least to the start of this session's live testing. `orchestrator@`'s IAM roles (Firestore, Pub/Sub, Secret Manager, Vertex AI, Cloud Run Jobs-trigger) never included a Cloud Trace role at all — the `tracing.py`/`CloudTraceSpanExporter` code was correct, but every span had been silently dropped since Sprint 2.

**Resolution (Sprint 3):** granted `orchestrator@` → `roles/cloudtrace.agent` (the standard role providing `cloudtrace.traces.patch`). Flagged at the time as fixed-but-not-independently-reverified.

**Sprint 4 close-out's re-check — mixed result.** Queried the Cloud Trace REST API directly (`gcloud` has no `trace` command group in this environment) for the live conflicting-PR test's time window. IAM is confirmed correct: `roles/cloudtrace.agent` is present on `orchestrator@`, and ADK's own auto-instrumentation spans (`invocation`, `invoke_agent intake_agent`/`planning_agent`/`verification_agent`, `call_llm`, `generate_content gemini-3.7-flash`) **do** export successfully and were found in the trace query. But **zero spans named `gate.*`** (the custom `tracing.gate_span()` spans Phase 2.5/3.7/4.5 all rely on) were found anywhere in the same window, across Gate 1, 2, and 3 decisions from both live test tickets. So the Milestone 5 fix was necessary but not sufficient — something separate is preventing `gate_span`'s spans specifically from reaching Cloud Trace, while ADK's own spans (presumably using a different tracer/provider registration path) get through fine. No orchestrator log output (searched for "trace"/"tracer"/"TracerProvider") pointed at a specific cause; the leading hypothesis, not yet confirmed, is a `TracerProvider` double-registration issue — `opentelemetry.trace.set_tracer_provider()` is a one-time global operation, and if ADK's own instrumentation registers a provider before or instead of `tracing.setup_tracing()`'s, `trace.get_tracer("artisan.orchestrator")` could silently end up on a provider that isn't wired to our `BatchSpanProcessor`/`CloudTraceSpanExporter`. **Not fixed here** — this needs its own investigation (checking import/initialization order between `tracing.setup_tracing()` and ADK's auto-instrumentation) — flagged for Sprint 6's observability audit (Phase 6.1) rather than attempted as a drive-by during Sprint 4's close-out.

## Milestone Log

The narrative account below (bugs found, decisions forced, live-test outcomes) is complementary to [MILESTONE.md](./MILESTONE.md)'s phase-by-phase Definition-of-Done archive for the same closed sprints — this tells *how* each sprint went, that tells *what* was scoped and confirmed done.

### Milestone 0 — Planning (2026-08-28)
Wrote PRD, system design, and tech stack docs. Key decisions locked in:
- ADK agents in Python; dashboard in Next.js/React (TypeScript).
- Jira via `mcp-atlassian` service account (not per-user Jira login).
- GitHub via a GitHub App; dashboard login via GitHub OAuth.
- Execution Agent runs in ephemeral Cloud Run Jobs, one per attempt.
- Single monorepo, greenfield.

### Milestone 1 — Sprint 1 complete: infra + scaffold (2026-08-28)
Provisioned every external prerequisite (GCP project `artisan-multiagent-ai`, 3 least-privilege service accounts, 3 secrets, Jira project `ART`, GitHub App + demo repo), scaffolded all 3 code directories with passing tests/builds, created the Firestore native-mode DB, and deployed `mcp-atlassian` to Cloud Run. See "External accounts & identifiers" above for exact IDs. Forced decisions: `mcp-atlassian` pinned to 0.23.1, retry cap `N=3`, `google-adk` pinned to 2.8.0. One item deferred to Sprint 2: live MCP-protocol verification of `mcp-atlassian`, since testing an internal-ingress service from outside GCP isn't the real call path.

### Milestone 2 — Sprint 2 built + deployed: Gate 1 ticket-bootstrap confirmed live (2026-08-28)
Implemented all 5 phases of Gate 1 in `agents/`: webhook ingestion + Pub/Sub publish (`app.py`, `github/webhook.py`, `gcp/pubsub.py`), ticket bootstrap (`dispatch.py`, `gcp/firestore_client.py`, `jira/client.py`), the Intake Agent as a real ADK `Agent` (`agents/intake_agent.py`), the clarification loop with the transactional 3-round cap (`gcp/firestore_client.py::increment_clarification_round`), and per-gate-decision OTel tracing (`tracing.py`). Deployed to GCP: Pub/Sub topic/subscription, orchestrator Cloud Run service, IAM grants, GitHub App webhook URL pointed at the live endpoint. Live field-testing (real webhook deliveries, not just unit tests) caught three real bugs before they could reach a demo: (1) Cloud Run's default Pub/Sub push OIDC audience is the *full push endpoint URL including path* (`.../pubsub/push`), not just the service origin; (2) a Firestore transactional callable's writes only commit on normal return — raising inside it would have rolled back the very cap-flip it was supposed to persist, so `increment_clarification_round` returns a `(count, at_cap)` tuple and the caller raises after commit; (3) the pinned `mcp-atlassian:0.23.1` image has an unresolved auth bug (two independently-valid API tokens both failed identically through it) — dropped for direct Jira REST calls, proven with 6 real tickets created live (`ART-1`–`ART-6`, cleaned up after). Also caught: removing the `mcp` extra silently dropped `pyjwt`, needed for GitHub App JWT signing (fixed via `githubkit[auth-app]`). 34 pytest tests pass. **Ticket bootstrap (webhook → Jira ticket) is now confirmed live end-to-end; the Intake Agent → clarification/sufficient branches are still only unit-tested** — all live smoke tests used synthetic GitHub issue numbers that don't exist, so `get_issue_thread` 404s before reaching the agent. Next: verify against a real GitHub issue, then Sprint 3 (Gate 2 — Plan → Execute → Verify → PR).

### Milestone 3 — Sprint 2 closed: Gate 1 confirmed live on both branches (2026-08-28)

Closed the one gap left after Milestone 2 by opening two real GitHub issues on `403errors/artisan-demo` and watching them through the full live pipeline. Doing so surfaced two more real bugs, both fixed live:

1. **Gemini access was never actually configured** (`ValueError: No API key was provided`) — neither Vertex AI nor the Gemini Developer API was enabled on the project. Fixed by wiring Vertex AI: enabled `aiplatform.googleapis.com`, granted `orchestrator@` → `roles/aiplatform.user`, set `GOOGLE_GENAI_USE_VERTEXAI=TRUE`/`GOOGLE_CLOUD_PROJECT`/`GOOGLE_CLOUD_LOCATION` env vars on Cloud Run. Then hit a second layer of the same bug: `GOOGLE_CLOUD_LOCATION=us-central1` 404'd on `gemini-3.7-flash` specifically (confirmed `gemini-2.5-flash` worked fine on that region) — the model is only served from Vertex AI's `global` location, not discoverable from the Console UI. Fixed by setting `GOOGLE_CLOUD_LOCATION=global`.
2. **Pub/Sub infinite-retry incident** — separately discovered while debugging (1), predating it: no dead-letter policy on the push subscription meant a permanently-failing message (a synthetic test issue number that never existed on GitHub) had been retrying non-stop for ~95 minutes, ~4,400+ failed requests. Fixed with a dead-letter topic (`artisan-github-events-dlq`, max 5 attempts) and cleaned up the stuck Firestore ticket + Jira ticket (`ART-7`).

With both fixed, issue #2 (well-specified) went sufficient → real Gemini call → Jira `ART-8` → *In Progress*, and issue #3 (deliberately vague) went insufficient → real Gemini call → clarifying question posted → `clarification_rounds: 1`, Jira `ART-9` correctly left untouched. **Sprint 2 is now closed.** Next: Sprint 3 (Gate 2 — Plan → Execute → Verify → PR).

### Milestone 4 — Sprint 3 code-complete: Gate 2 built, not yet deployed/verified live (2026-08-28)

Implemented all 7 phases of Gate 2 in `agents/` and built `execution-sandbox/` from scratch (previously a bare stub): routing/domain-expert/planning/verification agents, `gate2.py`'s plan→execute→verify retry loop with a transactional retry cap and atomic escalation-history append, `gcp/cloud_run_jobs.py`'s synchronous trigger-and-await, and a bounded ADK coding agent that clones/codes/tests/pushes. Forced decisions (see the three "Known follow-up" entries above for the reasoning on each): (1) `agents/`/`execution-sandbox/` now share code via a new `uv` workspace + `packages/artisan_shared/` package rather than duplicating `Plan`/`ExecutionResult`/`ticket_doc_id`/GitHub-auth logic; (2) the Cloud Run Job execution is triggered and awaited synchronously inside the same `/pubsub/push` request (no new async completion infra); (3) the coding agent uses custom function tools, not ADK's built-in `bash_tool` (which requires human confirmation per command and would hang forever unattended). Also discovered live: this Jira project's real workflow has no "PR Open" status, so Gate 2 communicates PR-open via a Jira comment rather than a status transition. Both `agents/Dockerfile` (updated) and `execution-sandbox/Dockerfile` (new) now build from a repo-root context because of the workspace — verified via a local `uv sync` simulation of each Docker build stage (no Docker daemon available in this environment to build the images directly). 75 tests passing (`agents/`: 50, `packages/artisan_shared/`: 12, `execution-sandbox/`: 13). **Not yet done, and explicitly Sprint 3's Phase 3.4/3.6 DoDs by their own wording:** granting `execution-sandbox@` a `secretAccessor` IAM binding, deploying `execution-sandbox` as a Cloud Run Job for the first time, redeploying `orchestrator`, and running one real GitHub issue through the full Gate 1 → Gate 2 pipeline live. Next: perform that deploy + live verification to actually close Sprint 3, then Sprint 4 (Gate 3 — merge conflict triage).

### Milestone 5 — Sprint 3 closed: Gate 2 confirmed live end-to-end (2026-08-28)

Closed every item left open at Milestone 4. Two real bugs surfaced and got fixed along the way, on top of the two already-known deploy steps:

1. **Duplicate-delivery race in the idempotency guard** (see "Known follow-up" above) — `is_duplicate_delivery`/`mark_delivery_processed` replaced with a claim-before-process lifecycle (`claim_delivery`/`mark_delivery_completed`/`mark_delivery_failed`), found while planning this session's live test rather than by accident in production. 79 tests passing after this change (`agents/`: 54, `packages/artisan_shared/`: 12, `execution-sandbox/`: 13).
2. Both `agents/Dockerfile` and `execution-sandbox/Dockerfile` were built for real for the first time via **Cloud Build** (`gcloud builds submit` with an inline `cloudbuild.yaml`, since no local Docker daemon exists in this environment) — both succeeded on the first try, tagged `a89aa31-dirty` in the `cloud-run-source-deploy` Artifact Registry repo. (`orchestrator` was rebuilt a second time, `a89aa31-dirty2`, after Milestone 5 step 8's staleness-constant fix below.)
3. Granted `execution-sandbox@` → `secretAccessor` on `github-app-private-key`, deployed `execution-sandbox` as a Cloud Run Job for the first time (`--task-timeout=1800`, `ARTISAN_DEMO_REPO_TEST_COMMAND=true` — the demo repo has no real test suite yet, so the smoke test/live run use a no-op test command rather than spuriously failing on a missing `package.json`).
4. Redeployed `orchestrator` with the Sprint 3 code, `--timeout=3600`; also raised the push subscription's ack deadline to 600s.
5. **Manual smoke-test trigger of `execution-sandbox`** — first attempt failed with `ValueError: No API key was provided` (the coding agent's own Gemini call), because `execution-sandbox@` had never been granted `roles/aiplatform.user` — the exact Sprint-2 Gemini-wiring bug (Milestone 3), just never applied to this second service account. Fixed by granting the role and adding the same `GOOGLE_GENAI_USE_VERTEXAI`/`GOOGLE_CLOUD_PROJECT`/`GOOGLE_CLOUD_LOCATION=global` env vars to the job. Second attempt got further (real clone/code/test/push) but crashed on the final Firestore write — `write_execution_result` uses `.update()`, which requires the ticket doc to already exist, and the synthetic smoke-test issue number (`99001`) had never been through Gate 1's ticket bootstrap. Not a production bug (the orchestrator always creates the ticket first in the real flow) — fixed by pre-creating a scratch ticket doc, then got one fully clean run: real branch, real commit, `tests_passed=True`, correct Firestore write. Scratch doc/branch deleted afterward.
6. **One real GitHub issue end-to-end** (`403errors/artisan-demo#4`, "Add a simple static landing page") — went sufficient → Jira `ART-10` → *In Progress* → routing picked `frontend` → planning → execution → verification green **on the first attempt, zero retries** → real PR opened (`403errors/artisan-demo#5`, branch `artisan/ART-10-attempt-1`) → Jira comment posted with the PR link, ticket correctly left at *In Progress* per the already-documented workflow-status finding.
7. **`orchestrator@` was missing `roles/cloudtrace.agent`** (see "Known follow-up" above) — found while verifying step 6 produced Cloud Trace spans; every gate span had been silently failing to export since Sprint 2. Fixed, but not independently re-verified with a fresh trace (would have meant another live GitHub/Jira event solely to check tracing).
8. **`DELIVERY_CLAIM_STALE_AFTER_SECONDS` was set to a value shorter than the timeout it needed to outlast** — caught while writing this milestone entry, before it could bite in production. The first pass set it to `1800` (30 min) with a comment claiming it was "comfortably longer than the orchestrator's own request timeout," but that timeout had already been raised to `3600` (step 4) — 1800 < 3600 reopens the exact race the claim-before-process fix (item 1) closes, for any Gate 2 attempt legitimately running past 30 minutes. Fixed to `4200` (70 min, safely above the 3600s ceiling); rebuilt `orchestrator` (`a89aa31-dirty2`) and redeployed.

**Sprint 3 is now closed.** Next: Sprint 4 (Gate 3 — merge conflict triage).

### Milestone 6 — Sprint 4 code-complete: Gate 3 built, not yet deployed/verified live (2026-08-28)

Implemented all 5 phases of Gate 3 in `agents/` and extended `execution-sandbox/` (rather than building a second job): conflict detection via an authoritative trial merge (not GitHub's async `mergeable_state`), Conflict Agent classification, a capped-at-1 trivial-resolution attempt, and dual-notify (GitHub PR comment + Jira comment) escalation for both the semantic path and a failed/capped trivial attempt. Two real bugs were caught during design review, before any code shipped with them (see Current Status above for the full mechanics): (1) the merge direction — checkout **head**, merge **base** into it, never the reverse, since the reverse produces a non-fast-forward commit and would force a force-push, forbidden by PRD.md §5; (2) the trivial-conflict cap's comparison must be `>`, not `>=` (`increment_retry_round`'s comparison, copied naively, would have made the whole trivial-resolution path unreachable). Forced decisions: `execution-sandbox` gained a `JOB_MODE` env var (`execute`/`detect_conflict`/`resolve_conflict`) reusing the single existing Cloud Run Job resource, rather than a second job, specifically because `execution-sandbox@` is the only service account with GitHub-App-token-minting rights — doing the trial-merge in the orchestrator instead would have meant breaking that least-privilege split; PR→ticket lookup is a new `pr_index` pointer-doc collection (mirroring `ticket_ids.py`'s existing philosophy) rather than parsing `Resolves #N` out of the PR body. Also decided, deliberately, as accepted simplifications for this sprint (not oversights): concurrent `synchronize` deliveries on the same ticket aren't individually deduped beyond what the Firestore cap already guarantees (at most one resolution attempt ever succeeds); and Gate 3 never auto-recovers an already-`escalated` ticket even if a human's manual fix makes a re-check come back clean — that's a manual dashboard action later (Sprint 5). 116 tests passing (`agents/`: 75, `packages/artisan_shared/`: 13, `execution-sandbox/`: 28). **Not yet done, mirroring exactly Sprint 3's own close-out arc (Milestone 5):** building/redeploying both images live, and running one real conflicting-PR pair through the pipeline — not attempted yet since only the build/test phase was requested this round. Also worth flagging for whoever choreographs the Sprint 8 demo: GitHub's `mergeable_state`/conflict detection is always PR-vs-its-own-base, so the demo's "introduce a second, conflicting PR" needs the first PR's change already merged to the base branch *before* the second (also Artisan-opened, via Gate 2) PR is opened/synchronized — Gate 3 no-ops on any PR without a `pr_index` pointer, so a hand-authored demo PR outside Gate 2's flow won't trigger it. Next: perform that live deploy + verification to actually close Sprint 4, then Sprint 5 (monitoring dashboard).

### Milestone 7 — Sprint 4 closed: Gate 3 confirmed live, one real bug found and fixed (2026-08-29)

Also landed two small Sprint 6-scoped fixes ahead of the close-out rebuild so they'd ship in the same image: Gate 2's `_escalate` now also posts a short reporter-facing GitHub issue comment (previously Jira-only); `/pubsub/push` now classifies a GitHub 404 on `get_issue_thread` as `dispatch.NonRetriableEventError` and acks immediately instead of burning the dead-letter retry budget. 78/13/29 tests passing across the three packages after these two changes plus Milestone 6's baseline.

**Live deploy:** rebuilt both images via Cloud Build from a clean commit (`f612a82`, no `-dirty` suffix), redeployed `execution-sandbox` (job) and `orchestrator` (service) — the orchestrator redeploy needed manual confirmation since it's a live, traffic-serving service. Manual smoke-test of the new `JOB_MODE=detect_conflict` path (a scratch ticket, `base=head=main`) round-tripped cleanly before running a real test.

**Live conflicting-PR test #1 — found and fixed a real bug.** Issue `403errors/artisan-demo#4`'s PR (`#5`, adds `index.html`) was still open/unmerged; opened a second issue (`#6`) for a similarly-worded landing page, let it run through Gate 1→2 independently (`PR #7`, based on pre-merge `main`), merged PR `#5`, then pushed a follow-up commit onto PR `#7`'s branch to force a `synchronize` (merging a PR into base doesn't itself notify an unrelated open PR — no such webhook exists, matching Milestone 6's documented "accepted simplification"). This produced a genuine add/add conflict on `index.html` plus a content conflict on `README.md` — but Gate 3's detection reported `has_conflict: true` via the code's fail-safe path with **empty** `conflicted_files`/`conflict_markers`, because `execution-sandbox`'s trial-merge subprocess crashed with `fatal: ... unable to auto-detect email address` / "Committer identity unknown." Root cause: `git_ops.py`'s `merge()` never passed a git identity to the subprocess, unlike `commit_all()` (which already uses `_COMMIT_AUTHOR_ARGS` for exactly this reason) — some environments (the container, not reproduced locally on macOS git 2.39.5 with the same repo content) require committer identity to attempt a `--no-ff` merge even with `--no-commit`. The trivial-conflict cap and escalation logic still worked *correctly* on this corrupted input — capped at exactly 1 resolution attempt, escalated with a dual GitHub PR comment + Jira comment — proving the safety nets sound even though the underlying conflict data was garbage. **Fixed:** `merge()` now passes the same `_COMMIT_AUTHOR_ARGS` as `commit_all()`; added `test_merge_passes_bot_identity_to_the_subprocess` (asserts the identity flags reach the subprocess call directly, since the failure mode isn't reliably reproducible outside the actual container). Rebuilt/redeployed `execution-sandbox` (`f188f76`).

**Live conflicting-PR test #2 — confirmed the fix.** Opened a fresh pair (`#8`→PR `#9`, `#10`→PR `#11`) both editing the *same* existing hero-tagline line in `index.html`/`README.md` (now that `main` has a real landing page from test #1). Merged PR `#9`, pushed a follow-up commit to PR `#11`'s branch. This time detection was clean: real `conflicted_files: ["README.md", "index.html"]`, real `<<<<<<<`/`=======`/`>>>>>>>` markers, real `base_branch_history`. The Conflict Agent correctly classified it `semantic` (same line, differing intent — "autonomous multi-agent platform on Google Cloud" vs. "Powered by Gemini and Vertex AI") both times it ran (see next paragraph), and escalated with a genuine Side-A/Side-B comparison on both GitHub and Jira.

**Two new gaps found live, not fixed here, flagged for Sprint 6:**
1. **Semantic escalation has no dedup cap.** PR `#11` received **two** near-identical "needs manual pickup" comments — Gate 3 ran independently for the `pull_request.opened` event and again for the `synchronize` event from the forced follow-up push, and the semantic path has no Firestore-backed guard against repeat escalations the way the trivial path's `trivial_conflict_attempts` cap does. Milestone 6's "accepted simplification" note about undeduped `synchronize` deliveries assumed the worst case was "one wasted detection job" — this shows the semantic path's worst case is actually duplicate maintainer-facing notifications on both GitHub and Jira.
2. **A `pull_request.opened`/`write_pr_pointer` race can silently no-op Gate 3's first check.** In test #1, PR `#7`'s own `opened` webhook was processed by the orchestrator ~1s after the PR was created — closely racing `_open_pr_and_sync`'s own `write_pr_pointer` call a few lines later in the same Gate 2 request. `get_ticket_by_pr` found no pointer yet and no-op'd; the pointer existed moments later when checked directly. Harmless here (no conflict could exist yet at PR-open time regardless), but if a real conflict existed at PR-open time and no `synchronize` ever followed, Gate 3 could miss it entirely.

**Cloud Trace re-check:** see the updated "Known follow-up" entry above — IAM is fine, but custom `gate.*` spans still aren't exporting (ADK's own spans do). Not fixed here; Sprint 6 scope.

**Sprint 4 is now closed.** Next: Sprint 5 (monitoring dashboard).

### Milestone 8 — Sprint 5 built: monitoring dashboard, read-only + live (2026-08-29)

Built out `dashboard/` from the bare Sprint 1 scaffold into a working Next.js 15 app: Auth.js v5
GitHub OAuth sign-in (a new, separate OAuth App from the GitHub App), a card-grid ticket overview
(`/tickets`) with a status tag per ticket ("Being Handled by Artisan", "Needs Manual Review",
etc.), a full gate-by-gate drill-in page (`/tickets/:id`) showing plan/execution/conflict data as
summaries + metrics (never raw logs — `logsUri` is always an external "view full logs" link, per
the schema's existing summary/pointer split), an "awaiting human" filtered view (`/escalations`),
and SSE-based live updates bridging server-side Firestore `onSnapshot` listeners to the browser.
shadcn/ui over Tailwind 4 for the component layer.

Two backend gaps found during design were fixed as part of this sprint rather than deferred,
since the dashboard's value depends on them: (1) **`trace_ids` was dead code** — `tracing.gate_span`
opened real Cloud Trace spans but never wrote the trace id back to Firestore; it's now an
`asynccontextmanager` that does so on exit via a new `firestore_client.append_trace_id` (mirroring
`append_escalation`'s `ArrayUnion` shape, but deliberately not flipping `status`). All 14 call
sites (`dispatch.py` x3, `gate2.py` x4, `gate3.py` x7) changed from `with` to `async with` —
mechanical, since `span` was never bound with `as span` anywhere. (2) **No live "what's
happening" signal existed** — Firestore was only written to at gate *decision* points, not
sub-steps in between — so a new `current_step` field (plain `str | None`, e.g.
`"planning (attempt 2)"`, `"detecting_conflict"`) is now written via the existing generic
`update_ticket(**fields)` at each sub-step transition in `dispatch.py`/`gate2.py`/`gate3.py`.

Two real bugs surfaced live, during Playwright verification against the actual production build
(not just unit tests), both fixed: (1) `export { auth as middleware } from "@/auth"` silently
broke Next 15's Turbopack static extraction of `config.matcher` — it fell back to matching every
path, and combined with an `authorized` callback (which turned out to affect every `auth()` call
app-wide, not just middleware-matched paths) turned every `/api/tickets*` 401 into an unusable
redirect. Fixed by dropping middleware entirely in favor of explicit per-page checks
(`dashboard/src/lib/require-session.ts`) — simpler and avoids the Turbopack/Auth.js interaction.
(2) Auth.js v5 needs `trustHost: true` for any non-default-port/host, including local dev and
(later) Cloud Run's dynamic `*.run.app` hostname. Also created (via `gcloud`, not yet in IaC —
flagged for Sprint 7) the two Firestore composite indexes the dashboard's queries need: `(github_repo
ASC, updated_at DESC)` and `(github_repo ASC, status ASC, updated_at DESC)`.

Verified end-to-end against the real `artisan-multiagent-ai` Firestore data from Sprints 2–4's
live tests (`ART-8`/`ART-9`/`ART-10`): 20 Vitest component/unit tests and all 6 Playwright e2e
specs (unauthenticated 401 + redirect, test-provider sign-in, ticket list render, ticket detail
render) pass against a real production build (`pnpm build && pnpm test:e2e`). 79 `agents/` pytest
tests still green after the `trace_ids`/`current_step` changes.

**Deliberately deferred, not built this sprint:** a manual "re-check/un-escalate" action on an
already-escalated ticket (`CONTEXT.md`'s own Milestone 6 flagged this as a possible Sprint 5 item,
but `MILESTONE.md`'s actual Sprint 5 phase list and PRD F5's acceptance criteria are both read-only —
building a mutation endpoint is a separately-scoped feature: it needs its own write-authorization
question and a new Gate-3 re-entry point that doesn't exist today). Pick this up in Sprint 6/7
alongside the other two flagged Gate-3 gaps (semantic-escalation dedup, the
`pull_request.opened`/`write_pr_pointer` race).

**Sprint 5 is closed.** Next: Sprint 6 (observability, security & hardening) — see
[SPRINT.md](./SPRINT.md#sprint-6--observability-security--hardening) for the now-longer worklist
(Cloud Trace custom-span export, semantic-escalation dedup, the PR-pointer race, plus a full
cap/idempotency regression pass).

### Milestone 9 — Sprint 6 code-complete: agent execution event log + manual action API built, not yet deployed/verified live (2026-08-29)

Built the two backend pieces Sprint 5's dashboard couldn't show without them: a real per-ticket execution trace, and the ability to act on a ticket from the dashboard instead of only watching it.

**Agent execution event log.** A new `tickets/{ticketId}/events/{autoId}` Firestore subcollection (deliberately not an array field on the ticket doc — Firestore's ~1MiB per-document cap plus `ArrayUnion`'s silent-dedup behavior made that unsafe for an audit log) records gate decisions, step transitions, agent invocations/completions, escalations, Cloud Run Job triggers, and — the actual payoff — every coding-agent tool call with its result, via `execution-sandbox/coding_agent.py`'s previously-discarded ADK event stream (`event.get_function_calls()`/`get_function_responses()`, correlated by `FunctionCall.id`). Secrets are redacted and payloads truncated before every write. Ordering uses Firestore's `SERVER_TIMESTAMP` (not a client-computed ISO string, this repo's usual convention) because the orchestrator and execution-sandbox are different processes with unbounded clock skew. An `EVENT_LOG_ENABLED` flag (both services) and a `NoOpEventSink` default make the whole thing killable without touching gate control flow. Found and fixed a real duplication bug along the way: `intake_agent.py`'s `run_intake` had its own byte-for-byte copy of `run_structured`'s session boilerplate, predating that helper's extraction — refactored to use it, so Gate 1 is no longer invisible to the trace.

**Manual action API.** `manual_actions.py` (retry Gate 1/2/3, force-escalate, mark resolved) and a new `completion.py::mark_ticket_done`, published from the dashboard to the *same* `artisan-github-events` Pub/Sub topic real GitHub webhooks use — reusing OIDC-authenticated ingress, at-least-once delivery, and `claim_delivery`'s idempotency rather than building a second mechanism, discriminated by a new `kind` field on the Pub/Sub envelope. Found and fixed a real latent bug: `gate2.py`'s branch name (`artisan/{jira_key}-attempt-{n}`) collides with a branch a prior run already pushed once a manual retry restarts the attempt counter at 1 — fixed by folding a new `manual_retry_generation` counter into the branch name. Also closed a real, previously-undocumented gap: **nothing in the codebase ever set a ticket to `done`**, despite `docs/SYSTEM_DESIGN.md` §9 claiming the merge→Done transition was "enforced in the orchestrator" — it wasn't. `mark_ticket_done` now has two callers: a new `pull_request.closed && merged` branch in `dispatch.handle_event` (the webhook was already flowing through Pub/Sub and being silently dropped), and the manual "mark resolved" action — built as one idempotent function with two triggers specifically so they can't race each other.

250 tests passing across all four packages (`packages/artisan_shared`: 34, `agents`: 100, `execution-sandbox`: 33, `dashboard`: 83), plus a clean production build. All new/changed backend tests are unit-level, mocking `firestore_client`/`jira_client`/`github_client`/the Firestore client directly — deliberately did not run `agents/tests/test_firestore_client.py` (it's a real integration suite against the live `artisan-multiagent-ai` database) or any live GCP call this round.

**Not yet done:**
1. ~~The dashboard's action buttons were never clicked in a live browser.~~ **Resolved (2026-08-29):** manually smoke-tested against a real ticket.
2. ~~The dashboard's service account does not yet have `roles/pubsub.publisher`~~ **Resolved (2026-08-29):** `roles/pubsub.publisher` on `artisan-github-events` granted to `dashboard@artisan-multiagent-ai.iam.gserviceaccount.com` via `gcloud pubsub topics add-iam-policy-binding`. No other new IAM/index/topic is needed — the dashboard stays Firestore read-only otherwise, exactly as `SYSTEM_DESIGN.md` §8 documents.
3. ~~Building/redeploying `orchestrator`/`execution-sandbox`/`dashboard` images and running one real manual action through the full pipeline live~~ **Partially resolved (2026-08-29):** `orchestrator` and `execution-sandbox` rebuilt via Cloud Build and redeployed at commit `4bed365` (orchestrator revision `orchestrator-00011-f5z`, both serving 100% traffic / job updated). **Real gap found doing this:** the dashboard has never actually been deployed to Cloud Run at all — there is no `dashboard/Dockerfile`, and `gcloud run services list` shows only `orchestrator`; this milestone's earlier note that "the dashboard image needs to pick up no code change" was wrong on that basis, since there is no dashboard image or service yet to begin with. Live use so far has only ever been `pnpm dev` against the real `artisan-multiagent-ai` project's Firestore/Pub/Sub via ADC. Deploying the dashboard to Cloud Run (Dockerfile + service + moving its OAuth creds off `.env.local` into Secret Manager) is Sprint 7 scope, not done here.

Next: redeploy all three images (the dashboard image needs to pick up no code change, but `orchestrator`/`execution-sandbox` should already be current from Milestone 7 — confirm), then do the live smoke-test in item 1 to actually close Sprint 6.

### Milestone 10 — Sprint 6 closed: Cloud Trace root-caused and fixed, two Sprint-4 gaps closed, IAM tightened, full happy-path confirmed live (2026-08-29)

**Cloud Trace `gate.*` span export, root-caused (not just documented as broken).** Built a diagnostic `orchestrator` image logging `TracerProvider` identity around `setup_tracing()`'s registration and inside `gate_span()`, deployed it live, and triggered real Gate 1 decisions. Evidence ruled out the leading hypothesis from Milestone 7/9: `google-adk==2.8.0`'s own `telemetry/setup.py::maybe_set_otel_providers()` is only reachable from `adk/cli/api_server.py`, which this deployment never runs, and no other dependency calls `set_tracer_provider` — the diagnostic logs confirmed `setup_tracing()`'s provider registration wins every time (`ours won = True`), and `gate_span()` runs on that exact same provider object. So there was never a double-registration race. The real gap: `BatchSpanProcessor`'s default 5s async batching delay racing Cloud Run's request-scoped/scale-to-zero instance lifecycle — nothing forced an export before an idle instance could be paused, so queued spans could silently never flush. Fixed with one line: `trace.get_tracer_provider().force_flush(timeout_millis=5000)` at the end of every `gate_span()` (`agents/src/artisan_agents/tracing.py`). Confirmed live end-to-end: `gate.1.proceed`, `gate.1.ask`, `gate.1.escalate`, and `gate.2.proceed` spans are now queryable via the Cloud Trace v1 REST API, and a real ticket's `trace_ids` field was cross-checked to contain a trace ID actually found there.

**Two Sprint-4-flagged gaps, closed.** (1) Semantic-escalation dedup: `TicketDoc` gained `semantic_conflict_escalated: bool = False`; `firestore_client.claim_semantic_conflict_escalation` is a transactional one-shot claim (not a counter — there's no legitimate "retry" concept for a semantic escalation, unlike the trivial-conflict cap) gating `gate3.start_gate3`'s `_escalate_semantic` call, so a repeated `opened`/`synchronize` delivery reclassified `semantic` no longer re-posts duplicate GitHub+Jira comments. (2) The `pull_request.opened`/`write_pr_pointer` race: `gate2._open_pr_and_sync` can only write the `pr_index` pointer *after* GitHub assigns the PR number, so the ordering can't be reversed — absorbed on the consuming side instead: `gate3.handle_pull_request_event` now retries the pointer lookup up to 3 times (1s apart) before treating a PR as untracked.

**IAM least-privilege audit (Phase 6.2) — two real over-broad grants found and fixed on `orchestrator@`.** A project-level `roles/secretmanager.secretAccessor` was redundant with (and broader than) the three already-correct per-secret bindings — removed, relying solely on the per-secret grants. `roles/pubsub.editor` was broader than needed (orchestrator only ever publishes, never manages topics/subscriptions at runtime) — downgraded to `roles/pubsub.publisher`. `execution-sandbox@` and `dashboard@` both checked out exactly as `SYSTEM_DESIGN.md §8` already documented — no changes needed.

**Secret Manager audit (Phase 6.3) — clean.** Repo-wide grep for credential-shaped literals found nothing real (only the event log's own redaction regexes, e.g. `re.compile(r"ghp_[A-Za-z0-9]{36}")`, which look like tokens by design). `dashboard/.env.local` confirmed gitignored (`dashboard/.gitignore:34`) and never committed (`git log --all --full-history` on it is empty). The one real finding — the dashboard's OAuth App credentials (`GITHUB_ID`/`GITHUB_SECRET`/`AUTH_SECRET`) were still local-only — was unchanged from Milestone 9 at the time of this audit; since fixed pre-Sprint-7 (see Current Status above), not as part of this audit.

**Cap/idempotency regression pass (Phase 6.4) — clean, plus the two fixes above.** Full suite across all four workspace packages: 117 (`agents`) + 33 (`execution-sandbox`) + 34 (`packages/artisan_shared`) + 83 (`dashboard`) = 267 tests, all green, including new tests for both Sprint-4 gap fixes and the pre-existing forced-failure/duplicate-delivery regression tests from Sprints 2–4.

**Full happy-path integration test (Phase 6.5) — confirmed live, doubled as the trace-fix verification.** Real run on `403errors/artisan-demo`: issue #16 (well-specified) → Jira `ART-16` *In Progress* → Gate 1 `proceed` → Gate 2 (routing → planning → execution → verification) → PR #17 opened → merged → `pull_request.closed && merged` drove `completion.py::mark_ticket_done` → Firestore `status: done` → Jira `ART-16` flipped to **Done**. The Gate 1 decision's trace ID from this exact run is the one confirmed present in Cloud Trace above.

**Redeployed:** `orchestrator` at the final code state above (this session's diagnostic-tagged image, `orchestrator:trace-diag`, ended up carrying the real fix after diagnostics were stripped back out — re-tag to a commit hash once this work is committed, per this repo's usual convention). `execution-sandbox` and `dashboard` unaffected by this session's changes (no execution-sandbox/dashboard code touched).

**Sprint 6 is closed.** Next: Sprint 7 (deployment & CI/CD) — see [SPRINT.md](./SPRINT.md#sprint-7--deployment--cicd). Its one real blocker, confirmed by this milestone's Phase 6.3 audit: move the dashboard's OAuth App credentials off `.env.local` into Secret Manager before the dashboard can run as a real Cloud Run service (which, per Milestone 9, doesn't exist yet either — no `dashboard/Dockerfile`).

### Milestone 11 — Pre-Sprint-7 robustness hardening: intake/injection/repo-grounding/security-scan overhaul (2026-08-29)

A code-level audit (not a docs review) found real gaps between what the pipeline claimed to do and what it actually did, and the owner chose to close all of them before starting Sprint 7 rather than defer. Six workstreams landed, in dependency order:

1. **Intake overhaul.** `IntakeVerdict` replaced `sufficient: bool`/`missing_context_question: str | None` with `verdict: Literal["sufficient","needs_info","not_actionable"]` + `missing_context_questions: list[str]` (up to 3, posted as one numbered GitHub comment instead of one question burning one clarification round each). A genuinely off-topic issue (e.g. "how are you?") now gets `not_actionable` — a direct `manual_pickup` via a new `firestore_client.mark_manual_pickup_directly`, skipping the clarification-round counter entirely instead of wasting 3 rounds pretending it's a vague bug report. Issue/comment markdown images are now downloaded (`github_client.extract_and_download_images`, capped at 3 successes / 5MB each) and passed into Gemini as inline image parts via a new `images` param on `run_structured`; a >3-image issue short-circuits to a new `needs_human_review` ticket status (surfaced on the dashboard's `/escalations` view) before the Intake Agent ever runs.
2. **Prompt-injection isolation + shell allowlist.** New `artisan_shared.prompt_safety` (`wrap_untrusted`/`UNTRUSTED_CONTENT_NOTICE`/`flag_possible_injection`) wraps all untrusted issue/comment/feedback text fed into intake, domain-expert, planning, and coding-agent prompts, with a non-blocking heuristic flag surfaced via the event log. `execution-sandbox`'s `run_shell_command` tool was rewritten from a 3-string blocklist around full `shell=True` to a fail-closed allowlist (`npm`/`pnpm`/`python`/`pytest`/`node`/`yarn`, plus a restricted `git` subcommand allowlist) with `shell=False` argv execution — closes the shell-metacharacter/IFS bypass class the old blocklist was vulnerable to.
3. **Repo Context.** New `RepoContext` model + `agents/src/artisan_agents/repo_context.py::get_repo_context` fetches and Firestore-caches (`repo_context/{repo}`, 6h TTL or head-SHA-changed refetch) a repo's file tree, known-manifest contents, and a language histogram via new GitHub Git-Trees/Contents API client calls — the foundation that let routing/domain-expert/planning stop guessing at repo structure.
4. **Domain generalization.** `RoutingDecision.domains` and `DomainExpertOutput.domain` widened from a closed 3-value `Literal` to open `str`, so a non-web repo gets routed to a fitting domain name (e.g. `mobile`/`data-ml`/`cli`) instead of being force-fit into `frontend`/`backend`/`infra-devops`; `RoutingDecision` gained `subproject` for a basic monorepo signal. `backend`/`infra-devops` personas got real lenses (API contracts/idempotency; deployment/rollback/secrets) matching `frontend`'s existing depth, with a generic fallback lens for any domain routing invents.
5. **Planning Agent overhaul.** `planning_agent` now runs with `ThinkingConfig(thinking_level="HIGH")` (Gemini 3.7 Flash supports both a numeric `thinking_budget` and this enum; picked the enum to avoid guessing a token ceiling). `Plan` gained `removed_code: list[RemovedCodeItem]` (file/symbol/reason) — the plan must now name real stale code for the coding agent to delete in the same change, grounded in the real `RepoContext` file tree rather than guessed paths. The test_cases/doc_updates self-retry, previously gated on a `frontend` domain being present, now fires for any domain with an empty plan.
6. **Pre-push security scan + ready-for-review labels.** New `execution-sandbox/security_scan.py`: `gitleaks` (secret detection, hard-blocks the push on a hit) and `semgrep --config=p/security-audit` (static analysis, blocks only on `ERROR` severity, non-blocking findings surfaced in the diff summary) run between commit and push in both Gate 2's `run_attempt` and Gate 3's `run_conflict_resolution`; a best-effort `scan_new_dependencies` flags newly-added manifest dependencies for human visibility. Both tools fail open (never block a push) if the binary is missing or its output is unparseable — a broken scanner must not become an availability outage. `execution-sandbox/Dockerfile` now installs a pinned `gitleaks` v8.21.2 binary; `semgrep` added as a pip dependency. Gate 2's `_open_pr_and_sync` now applies a `artisan:ready-for-review` GitHub PR label and an `artisan-pr-open` Jira label (both best-effort — a labeling failure never aborts an already-successful PR/Jira-comment flow), giving a human-visible "agent is done, please review" signal beyond the raw ticket status.

267 → 267 pre-existing tests still green plus new coverage per workstream: `packages/artisan_shared` 41, `agents` 169, `execution-sandbox` 57 (267 total across the three Python packages), `dashboard` 83 Vitest — no regressions. One pre-existing (not introduced here) flaky `test_firestore_client.py` GCP-client fork/env flake was reproduced and confirmed unrelated to this work.

**Merged and deployed (2026-08-29).** Landed via PR #1 (`worktree-sprint7-hardening` → `main`, merge commit `3d5551a`). Both images rebuilt via Cloud Build (no local Docker daemon, same as every prior sprint) and redeployed from that PR's commit (`b87aacf`) ahead of the merge actually landing (Cloud Run deploys from a built image, not from `main` directly, so this was safe to sequence either order): `orchestrator:b87aacf` → revision `orchestrator-00016-w2v`, 100% traffic, `Ready`; `execution-sandbox:b87aacf` registered as the Cloud Run Job's image, existing config (`ARTISAN_DEMO_REPO_TEST_COMMAND=true`, service account, other env vars) preserved across the update.

**Live-verified, all four scenarios confirmed working:** an off-topic issue correctly landed `not_actionable`/`manual_pickup` without consuming a clarification round; a multi-ambiguity issue got a single numbered-list comment instead of one question at a time; a 4+ image issue short-circuited to `needs_human_review` before the Intake Agent ran; and a full Gate 2 run opened a PR carrying the `artisan:ready-for-review` GitHub label and Jira ticket carrying `artisan-pr-open`. This milestone is now fully closed — code-complete, unit-tested (267 Python + 83 dashboard tests), deployed, and live-verified.

### Milestone 12 — Issue-deleted terminal handling: `issues.deleted` webhook, mid-flight race guards, number-reuse correctness (2026-08-30)

The PR-merge terminal case has been real since Sprint 6 (Milestone 9/10: `pull_request.closed && merged` → `completion.mark_ticket_done` → Firestore `done` + Jira `Done`). The *issue-deleted* case was a genuine gap: `issues.deleted` webhooks were already flowing through Pub/Sub (`issues` is a subscribed event) but `dispatch.handle_event` silently dropped the action, so a deleted issue left its Firestore doc and Jira ticket open forever — and, worse, a deleted issue frees its number for reuse while the ticket doc is keyed by `(repo, issue_number)`, so a *future* issue reusing the number would have inherited the dead ticket's stale plan/PR state and never been processed. All closed in this pass:

1. **`issues.deleted` → `completion.handle_issue_deleted`.** No ticket → no-op (mirrors the untracked-PR no-op). Ticket exists → Firestore `status: done` + `current_step` cleared + new `issue_deleted` event; any open Artisan PR closed (`github_client.close_pull_request` — comment first, then `pulls.update(state=closed)`; best-effort, new `pr_closed` event, since it's Artisan's own PR, PRD §5's ownership rule holds); Jira comment + `Done` transition (best-effort, never rolls back Firestore). Same idempotent "no-op if already `done`" shape as `mark_ticket_done`, so a real merge and a deletion racing each other converge.
2. **Mid-flight race closed at three guard points.** (a) Gate 1's 404 on `get_issue_thread` now runs the same cleanup *before* re-raising `NonRetriableEventError` — the delivery is acked (never retried) and the ticket isn't left stuck in `intake`; (b) Gate 2 checks ticket status at the top of each retry-loop iteration and before `_open_pr_and_sync` — `done` mid-Gate-2 can only mean deletion (no PR exists yet, so a merge is impossible), so no sandbox run, PR open, or escalation happens on a dead issue; (c) Gate 2's `_escalate` skips entirely when `done`, so a deletion during a failing run can't flip `done` back to `escalated` or 404 on the reporter-facing comment.
3. **Number-reuse correctness.** The cleanup deliberately lands the old doc in the same terminal `done` a merge would, and `_handle_issue_opened` treats any pre-existing `done` doc on a fresh `opened` delivery as a reused number — creating a brand-new Firestore doc + Jira ticket — since a *live* resolved issue keeps its number forever, so `done` + a new `opened` can only mean the original was deleted.
4. **Shared `dispatch.infer_gate`** extracted from `manual_actions._infer_gate` (same `currentGate` mirror) so the `issue_deleted` event sink and manual escalations tag the same gate the dashboard would show; dashboard's event presentation gained `issue_deleted`/`pr_closed` entries.

**Code + tests + docs only — not yet deployed** (this is a code-level hardening pass ahead of Sprint 7's deploy; a Cloud Run rebuild is not required for a milestone to be code-complete, per prior milestones' precedent — flagging it here so Sprint 7's Phase 7.4 smoke test picks it up). Test count after this pass: `agents` 188 (was 169), `packages/artisan_shared` 41 (unchanged), `dashboard` 83 Vitest (unchanged) — all green, no regressions.

*(Add the next milestone below as it completes — keep entries short: what shipped, what decisions it forced, what's next.)*

## External accounts & identifiers (Sprint 1, Phases 1.1–1.3 — done)

- **GCP project:** `artisan-multiagent-ai`, billing linked (`My Billing Account 2`). APIs enabled: Cloud Run, Pub/Sub, Firestore, Secret Manager, Cloud Trace, Logging, Cloud Build, IAM, Artifact Registry.
- **Service accounts:** `orchestrator@` (datastore.user, pubsub.editor, secretAccessor, run.developer, `aiplatform.user` for Vertex AI, `iam.serviceAccountTokenCreator` for Pub/Sub's push service agent — added in Sprint 2, `cloudtrace.agent` — added in Sprint 3's close-out after tracing was found silently broken since Sprint 2), `execution-sandbox@` (datastore.user, `secretAccessor` on `github-app-private-key` only, `aiplatform.user` for the coding agent's own Gemini calls — both added in Sprint 3's close-out), `dashboard@` (datastore.viewer) — all least-privilege per [SYSTEM_DESIGN.md §8](./SYSTEM_DESIGN.md#8-auth--security).
- **Vertex AI:** `aiplatform.googleapis.com` enabled Sprint 2. Orchestrator's Gemini calls run via `GOOGLE_GENAI_USE_VERTEXAI=TRUE`, `GOOGLE_CLOUD_PROJECT=artisan-multiagent-ai`, `GOOGLE_CLOUD_LOCATION=global` (must be `global`, not a region — see Milestone 3). No API key/secret involved.
- **Pub/Sub dead-letter:** topic `artisan-github-events-dlq`, `max-delivery-attempts=5` on `artisan-github-events-push` (added Sprint 2 after an infinite-retry incident — see Milestone 3).
- **Secrets in Secret Manager:** `jira-api-token`, `github-webhook-secret`, `github-app-private-key` — all scoped to `orchestrator@` only via per-secret IAM bindings. `jira-api-token` has been rotated twice during Sprint 2 debugging (versions 1–3 all `enabled`); current value confirmed working via direct REST call.
- **Jira:** site `pieisnot22by7.atlassian.net`, project key `ART` (id 10034), team-managed Kanban template. Orchestrator authenticates directly as the Atlassian account `pieisnot22by7@gmail.com` + its API token (Basic Auth, REST) — no MCP intermediary as of Sprint 2.
- **GitHub:** demo repo `403errors/artisan-demo` (seeded with a README). GitHub App `artisan-bot-403errors`, App ID `4744770`, installed on `403errors` (installation id `157129507`). Webhook URL now points at the deployed orchestrator's `/webhooks/github` (updated from the Sprint 1 placeholder in Sprint 2).
- **Orchestrator Cloud Run service:** deployed in `us-central1`, public ingress (required for GitHub to reach `/webhooks/github`; `/pubsub/push` is separately protected by in-app OIDC verification), runs as `orchestrator@`, request timeout `3600s` (raised from the default 300s in Sprint 3's close-out).
- **`execution-sandbox` Cloud Run Job:** registered for the first time in Sprint 3's close-out, `us-central1`, runs as `execution-sandbox@`, `--task-timeout=1800`, `ARTISAN_DEMO_REPO_TEST_COMMAND=true` (the demo repo has no real test suite yet).
- **Images:** built via Cloud Build (no local Docker daemon in this environment) in the `cloud-run-source-deploy` Artifact Registry repo (`us-central1`). Sprint 3 close-out: `a89aa31-dirty`/`a89aa31-dirty2` (`-dirty` reflecting uncommitted changes at build time). Sprint 4 close-out (Milestone 7): `orchestrator:f612a82` + `execution-sandbox:f612a82` (clean commit, Gate 3 + the two Sprint-6-scoped fixes landed ahead of the rebuild), then `execution-sandbox:f188f76` (the git-identity fix found live, `orchestrator` unchanged since that fix is execution-sandbox-only). Sprint 6, Milestone 9 close-out: `orchestrator:4bed365` + `execution-sandbox:4bed365` (event log + manual action API), redeployed 2026-08-29 (`orchestrator` revision `orchestrator-00011-f5z`). Sprint 6, Milestone 10 close-out: `orchestrator:trace-diag` (the Cloud Trace fix + both Sprint-4 gap fixes; started as a diagnostic-only tag, ended up carrying the real fix once diagnostics were stripped back out) — re-tagged to `orchestrator:47c91e6` after commit and redeployed 2026-08-29 (`orchestrator` revision `orchestrator-00015-nb2`). 2026-08-30 deploy (commit `93bf62f`): `orchestrator:93bf62f` + `execution-sandbox:93bf62f` (clean commit — carries Milestone 12 issue-deleted handling, the Gate 2 default-branch fix, and Sprint 9's duplicate check), redeployed (`orchestrator` revision `orchestrator-00020-9gt`, 100% traffic; `execution-sandbox` job's image switched from legacy `gcr.io/artisan-multiagent-ai/execution-sandbox:latest` to the Artifact Registry path, env/service account/timeout preserved). No `dashboard` image exists yet — see Milestone 9 item 3.
- **Pub/Sub push subscription:** `ackDeadlineSeconds` raised to `600` (its ceiling) in Sprint 3's close-out, on top of (not instead of) the claim-based idempotency fix — see "Known follow-up" above.
- **Retry cap `N` = 3** (decided during Phase 1.5, matches the clarification-round cap).
- **`DELIVERY_CLAIM_STALE_AFTER_SECONDS` = 4200** (decided during Sprint 3's close-out idempotency fix — deliberately *longer* than the orchestrator's own 3600s/60min max request timeout, so a claim never goes stale while the underlying request could still legitimately be running; an earlier draft of this value, 1800, was caught and corrected during this same close-out for being shorter than the timeout it needed to outlast).
- **`MAX_TRIVIAL_CONFLICT_ATTEMPTS` = 1** (decided Phase 1.5, matches the design's stated cap; claimed via `increment_trivial_conflict_attempt`'s `>` comparison — see Milestone 6 for why `>=` would have been a bug here, unlike the other two caps).
- **Dashboard GitHub OAuth App** (Sprint 5, Milestone 8) — separate from `artisan-bot-403errors`. Client ID/secret + `AUTH_SECRET` live in `dashboard/.env.local` for local dev, and now also in Secret Manager (`dashboard-oauth-client-id`/`dashboard-oauth-client-secret`/`dashboard-auth-secret`, pre-Sprint-7). Wiring the deployed dashboard to read from Secret Manager (Cloud Run `--set-secrets`) is Sprint 7 scope.
- **Firestore composite indexes** (Sprint 5, Milestone 8, created via `gcloud`, not yet in IaC): `tickets` on `(github_repo ASC, updated_at DESC)` and `(github_repo ASC, status ASC, updated_at DESC)`.
- **IAM grant applied (Sprint 6, Milestone 9, 2026-08-29):** `dashboard@` granted `roles/pubsub.publisher` on the `artisan-github-events` topic, so the dashboard's manual-action routes (retry/escalate/mark-done) can now actually publish. `dashboard@` otherwise stays exactly `datastore.viewer` — no Firestore write grant, deliberately.
- **IAM tightened (Sprint 6, Milestone 10, 2026-08-29):** `orchestrator@`'s project-level `roles/secretmanager.secretAccessor` removed (redundant with the three correct per-secret bindings) and `roles/pubsub.editor` downgraded to `roles/pubsub.publisher` (it only ever publishes). Final `orchestrator@` project-level roles: `aiplatform.user`, `cloudtrace.agent`, `datastore.user`, `pubsub.publisher`, `run.developer`.
- **Milestone 11 close-out (2026-08-29):** `orchestrator:b87aacf` + `execution-sandbox:b87aacf` built via Cloud Build and redeployed (`orchestrator` revision `orchestrator-00016-w2v`, 100% traffic; `execution-sandbox` job's existing env vars/service account preserved across the image update). Merged to `main` via PR #1 (merge commit `3d5551a`). Live-verified: off-topic issue → `not_actionable`/`manual_pickup` with no clarification round consumed; multi-ambiguity issue → single numbered-question comment; 4+ image issue → `needs_human_review` short-circuit before Intake ran; one full Gate 2 run → PR carrying `artisan:ready-for-review` and Jira carrying `artisan-pr-open`.
- **Deploy 2026-08-30 (commit `93bf62f`):** `orchestrator:93bf62f` + `execution-sandbox:93bf62f` built via Cloud Build (build `25c25da9`) and redeployed, preserving existing config on both (orchestrator env/SA/3600s timeout/concurrency 80 intact — revision `orchestrator-00020-9gt` at 100% traffic; execution-sandbox env/SA/1800s timeout/maxRetries 3 intact — image migrated `gcr.io:latest` → Artifact Registry). Smoke-verified live: FastAPI serving, `/webhooks/github` + `/pubsub/push` both respond 405 on GET (routes registered). Ships Milestone 12's issue-deleted handling (`9985c4d`), the Gate 2 dynamic-default-branch fix (`0436916`), and Sprint 9's Gate 1 duplicate check (`6963564` + `93bf62f`).

## Open Decisions / Risks

- ~~`docs/SPRINT.md` untracked by git~~ **Resolved (Sprint 3 close-out): deliberate, not lingering.** The user explicitly confirmed `docs/SPRINT.md` should stay untracked/gitignored — it's a local-only planning doc, kept accurate on disk but never committed. Not an open risk; do not re-flag this without new information.
- Two Jira API tokens were pasted in plaintext into a terminal session during debugging (now sitting in that session's history/scrollback) — recommend rotating `jira-api-token` one more time from a clean terminal now that Sprint 2 is confirmed working, so no exposed value stays live.
- ~~The Pub/Sub dead-letter policy (added Sprint 2) is an infra-level mitigation only~~ **Partially resolved.** The specific known-permanent case (a GitHub 404 for an issue that doesn't exist — the exact incident that caused the ~95-minute retry storm) is now fixed at the code level: `dispatch.py` catches `githubkit.exception.RequestFailed` on `get_issue_thread`, and a 404 is re-raised as `dispatch.NonRetriableEventError`; `app.py`'s `/pubsub/push` handler acks it immediately (marks the delivery `completed`, returns 200) instead of letting Pub/Sub redeliver up to the dead-letter cap. **Refined (Milestone 12):** since a 404 now usually means the issue was deleted between webhook fire and delivery, the 404 path first runs `completion.handle_issue_deleted` (so the ticket isn't left stuck mid-gate), *then* acks — no longer "ack-only with zero side effects," and correctly so: the side effect is the same cleanup the `issues.deleted` webhook itself would run. Other non-retriable failure classes (not yet identified) still rely on the dead-letter policy as the sole backstop — this isn't a general exception-classification system, just this one confirmed case.
- Local Node is v23.7.0, not the TECH_STACK-pinned 22 LTS — worked fine for scaffold/build; revisit if a Node-version-specific issue surfaces later.
- Repo is git-initialized locally but nothing pushed yet — no GitHub remote chosen for the actual Artisan source repo (distinct from the throwaway `403errors/artisan-demo` target repo).
- ~~No Docker daemon available, neither image ever built~~ **Resolved (Sprint 3 close-out).** No Docker daemon exists in this environment either, so both Dockerfiles were built for real via Cloud Build instead of local `docker build` — both succeeded on the first try (see Milestone 5). `README.md`'s `docker build`/`docker push` commands are still accurate as the *local* path for anyone who does have Docker; Cloud Build is the equivalent for anyone who doesn't. Both images are now deployed and live.
- **Sprint 4 accepted simplifications, revised after Milestone 7's live test:** Gate 3 never auto-recovers an already-`escalated` ticket's status even if a human's manual fix makes a re-check come back clean — **deliberately deferred past Sprint 5 too** (see Milestone 8): a manual re-check/un-escalate action is a separately-scoped feature (new write-authorization question, no existing Gate-3 re-entry point), not something Sprint 5's read-only dashboard scope covers; no mechanism reacts to a *different*, unrelated PR merging into a base branch out from under an already-open PR (no such PR-side webhook exists — matches MILESTONE.md Phase 4.1's literal scope, and is exactly why Milestone 7's live test needed a manual follow-up push to force a `synchronize`). The "concurrent `synchronize` deliveries aren't deduped, worst case one wasted detection job" framing turned out to be **incomplete** — Milestone 7 proved the semantic-escalation path's actual worst case is duplicate maintainer-facing GitHub+Jira comments, not just a wasted job; see the new Sprint 6 item below.
- **New from Milestone 7, flagged for Sprint 6:** (1) semantic escalation has no dedup cap — every independent `opened`/`synchronize` delivery re-escalates from scratch, unlike the trivial path's Firestore-backed `trivial_conflict_attempts` cap; (2) a `pull_request.opened` webhook can be processed before Gate 2's own `write_pr_pointer` call lands a few lines later in the same request, silently no-op-ing Gate 3's very first check (harmless only because no conflict can exist yet at PR-open time regardless).
- ~~Gate 2's `_escalate` only posts to Jira, not GitHub~~ **Resolved.** `gate2.py`'s `_escalate` now also posts a short reporter-facing comment on the GitHub issue (not a PR — none exists yet at that point in Gate 2's loop) alongside the existing full-detail Jira comment. Deliberately not full parity with Gate 3's dual-notify: Gate 3's audience (a PR reviewer) may have no Jira access, so both systems get full detail; Gate 2's escalation is really "handed off to the Jira board," so Jira keeps the diagnostic detail and GitHub just tells the original reporter it's been handed to the team.
- **Sprint 8 demo-script constraint — now rehearsed twice (Milestone 7), still worth planning deliberately.** GitHub's conflict detection is always PR-vs-its-own-base, so the demo's "introduce a second, conflicting PR" needs the interfering change already merged into the base branch *before* the second PR is opened, and that second PR must itself be Artisan-opened (via Gate 2). Both live rehearsals additionally needed a manual empty-commit push onto the second PR's branch to force a `synchronize` event, since merging the first PR doesn't itself notify the second (see the new Sprint 6 item above) — the real demo recording will need the same nudge, or Gate 2 to make one more attempt/comment after the pair is chosen, chosen deliberately rather than improvised live.

### Milestone 13 — Sprint 7 closed: Declarative IaC, Dashboard Secret Management, CI/CD with Dashboard deploy (2026-08-31)

Closed all phases of Sprint 7:
1. **Dockerfiles (Phase 7.1):** Multi-stage Dockerfiles across all 3 components (`agents/Dockerfile`, `execution-sandbox/Dockerfile` with gitleaks v8.21.2 & semgrep, and `dashboard/Dockerfile` with Next.js 15 standalone output).
2. **Infrastructure as Code (Phase 7.2):** Full declarative Terraform topology in `infra/terraform/` (`versions.tf`, `variables.tf`, `apis.tf`, `service_accounts.tf`, `iam.tf`, `pubsub.tf`, `firestore.tf`, `secrets.tf`, `cloud_run.tf`, `wif.tf`, `outputs.tf`) plus one-command idempotent bootstrap script `infra/scripts/setup-gcp-infra.sh`.
3. **Dashboard Secret Management (Phase 7.2):** `infra/scripts/create-dashboard-secrets.sh` provisions `dashboard-oauth-client-id`, `dashboard-oauth-client-secret`, and `dashboard-auth-secret` in Secret Manager and assigns scoped `roles/secretmanager.secretAccessor` to `dashboard@`.
4. **CI/CD Automation (Phase 7.3):** `.github/workflows/ci.yml` (lint + 316 Python workspace tests + 110 Vitest tests + Next.js build) and `.github/workflows/deploy.yml` (automated build, push, and Cloud Run deploy of `orchestrator`, `execution-sandbox`, and `dashboard` via Workload Identity Federation on push to `main`).
5. **Deployment Runbook (Phase 7.4):** `docs/DEPLOYMENT.md`, `infra/README.md`, and root `README.md` updated with reproducible setup instructions.

### Milestone 14 — Hackathon Evaluator Access & Demo Onboarding (2026-09-01)

Configured seamless end-to-end evaluation access across all three platforms for official hackathon judging accounts (`testing@devpost.com`, `cloudhackathons@google.com`):
1. **Dashboard GitHub OAuth:** `hasRepoAccess` in `dashboard/src/lib/github-auth.ts` inspects both public profile and verified emails API (`/user/emails`), granting instant access to designated evaluator accounts without requiring repository collaborator invites.
2. **Google Cloud Trace IAM:** Granted `roles/cloudtrace.user` on project `artisan-multiagent-ai` to `group:testing@devpost.com` (`group:testing@challengepost.com`) and `group:cloudhackathons@google.com`.
3. **Sign-In Onboarding Guide:** `dashboard/src/app/signin/page.tsx` updated with clear evaluator instructions, direct link to the public demo repository (`403errors/artisan-demo`), and a note to accept the Jira invitation to view real-time Kanban board transitions.
4. **Incident — Gate 1 duplicate check crashed on every new issue (2026-09-01):** the deployed `orchestrator:93bf62f` (Sprint 9 duplicate check) called `gh.rest.search.async_search_issues(...)` in `github/client.py::search_similar_issues`, a method that does **not** exist on `githubkit==0.16.1`'s `SearchClient` (the real name is `async_issues_and_pull_requests`). Every `issues.opened` delivery crashed with `AttributeError` during intake, returned HTTP 500, was redelivered 5× and then dropped (`processed_deliveries` → `failed`), leaving the ticket stuck in `intake`/`evaluating_intake` — `403errors/artisan-demo#24` ("need the documentation", `ART-20`) was hit live and had to be manually escalated + marked done from the dashboard. The unit tests missed it because the `_FakeSearch` fake in `test_github_client.py` mirrored the made-up method name. **Fixed:** `client.py` now calls `async_issues_and_pull_requests`, the fake was renamed to match, and a new regression guard (`test_search_similar_issues_uses_a_real_githubkit_method`) introspects the real installed `SearchClient` to prove the called method exists — so a made-up method name can never pass CI again. The `intake`/`in_progress`/`escalated` tickets for issues #2/#3/#6/#10 predate the duplicate-check deploy and are unrelated to this crash.

## Next Milestone Target

**Sprint 7 is closed.** Next: **Sprint 8 (Post-Sprint Activity) — Demo, Docs & Submission** (architecture diagram submission asset, demo recording following the 6-step script, rubric self-check, written summary, Devpost submission).
